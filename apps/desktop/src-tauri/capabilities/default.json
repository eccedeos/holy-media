{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissoes da janela do operador. Mantidas no minimo: cada nova capacidade e' adicionada na fase que a usa.",
  "windows": ["control-room"],
  "permissions": ["core:default"]
}
